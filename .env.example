# 客户端可访问的服务地址，用于拼接 manifest 中的 asset URL
# Android 模拟器用 http://10.0.2.2:3000，iOS 模拟器/真机用实际 IP 或域名
HOSTNAME=http://10.0.2.2:3000

# RSA 私钥路径，用于对 manifest 进行签名；不设置则跳过签名
PRIVATE_KEY_PATH=code-signing-keys/private-key.pem

# 服务监听端口
PORT=3000

# OTA 发布产物存储目录
UPDATES_DIR=updates
